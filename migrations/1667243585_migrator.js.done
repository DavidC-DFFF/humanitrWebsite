module.exports = async function (deployer) {
  const Migrator = artifacts.require("Migrator");

  await deployer.deploy(
     Migrator);
  const contract = await Migrator.deployed();

  console.log("Contract address:", contract.address)
};